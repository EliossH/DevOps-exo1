services:
  storage:
    build: ./Storage
    volumes:
      - storageVolume:/logs
    ports:
      - "8080:8080"
  
  service2:
    build: ./Service2
    ports:
      - "8090:8090"
    depends_on:
      - storage
  
  service1:
    build: ./Service1
    ports:
      - "8070:8070"
    depends_on:
      - service2
      - storage

volumes:
  storageVolume: